import React from 'react';
//import MissingObject from '../MissingObjectModule/MissingObject/MissingObject';

var MissingObjects = (props) => {
    /*return(
        <section className = "posts">
            {props.posts.map((missingObject, postIndex) => {
                return (
                    <MissingObject
                        missingObject = {missingObject}
                    />
                )
            })}
        </section>
    );*/
}

export default MissingObjects;